# Test environment & failure summary

This top-level document points to the split summaries and comparison document created from the test logs.

Files:

- `test-env-npm.md` — local `npm test` run summary (from `logs/npm-test.log`)
- `test-env-ci.md` — CI run summary (from `logs/ci.log`)
- `test-env-matches.md` — matches and differences between the two runs

Use those files for focused triage. If you'd like, I can also produce a machine-readable list of hostnames/URLs found in both logs and create targeted Jest mocks for the top offenders.
